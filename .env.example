# Optional base URL override. Defaults to https://vr.org when unset.
# Only useful for pointing the server at a staging copy of the site.
# VR_ORG_BASE_URL=https://vr.org

# Optional suffix appended to the outbound User-Agent string (max 64 chars).
# VR_ORG_UA_SUFFIX=
